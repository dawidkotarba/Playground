[path]
http://localhost:8080/db

[h2 config]
Driver Class	org.h2.Driver
JDBC URL	jdbc:h2:mem:testdb
User Name	sa
Password	 <blank>

[Copy db to file]
SCRIPT NODATA TO 'h2db' --without data
SCRIPT TO 'h2db'

[useful queries]
// unmatched capitals
select * from capital left join country on capital.id = country.capital where country.name is NULL

// neighbours i.e for Poland
select * from country ct where ct.id in (select neighbour from neighbour nb where nb.country = (
select ct.id from country ct where ct.name = 'Poland'));